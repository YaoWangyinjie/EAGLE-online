CUDA_VISIBLE_DEVICES=0 python gen_sft_data.py \
    --dataset humaneval \
    --num_samples_per_question 1 \
    --temperature 0.0 \
    --output_file data/humaneval_sft_v1.jsonl \
    --max_new_tokens 4096

注意最大tokens数会导致部分推理答案被截断，检查生成的jsonl文件

CUDA_VISIBLE_DEVICES=0 python sft_main.py \
    --dataset humaneval \
    --num_epochs 200 \
    --lr 1e-5 \
    --save_every 20 \
    --train_id 0 \
    --resume_from auto \
    --trainpath data/humaneval_sft_v1.jsonl \
    --max_len 4096 \

CUDA_VISIBLE_DEVICES=0 python eval_sft.py \
    --dataset humaneval \
    --sft_eagle checkpoints/humaneval/0_1e-5_epoch_199 \
    --sft_only \
    --baseline_only \
    --run_tag epoch199 \
    --max_len 4096 \

进度：
1.sum,qa,alpaca效果好（可能是think链相似的原因）
2.用download_dataset.py下载测试小语种数据集
3.用download_dataset.py下载文学/法律等小众领域数据集：CNN/DailyMail XL-Sum GovReport BillSum MeQSum MLSUM ECtHR EURLEX LexGLUE
4.写pipeline，正常推理->发现al下降->分割train set，选取若干条query微调训练
6.小语种数据集的训练数据目前仍然是英文回答的，后续换成小语种语言重新测试
7.amharic/burmese两个数据集训练时设置max len=8192太小了，只有40句参与训练，但已经拟合且用时太长
