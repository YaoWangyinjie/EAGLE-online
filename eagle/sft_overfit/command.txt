CUDA_VISIBLE_DEVICES=0 python gen_sft_data.py \
    --dataset humaneval \
    --num_samples_per_question 1 \
    --temperature 0.0 \
    --output_file data/humaneval_sft_v1.jsonl \
    --max_new_tokens 4096

注意最大tokens数会导致部分推理答案被截断，检查生成的jsonl文件

CUDA_VISIBLE_DEVICES=0 python sft_main.py \
    --dataset humaneval \
    --num_epochs 200 \
    --lr 1e-5 \
    --save_every 20 \
    --train_id 0 \
    --resume_from auto \
    --trainpath data/humaneval_sft_v1.jsonl \
    --max_len 4096 \

CUDA_VISIBLE_DEVICES=0 python eval_sft.py \
    --dataset humaneval \
    --sft_eagle checkpoints/humaneval/0_1e-5_epoch_199 \
    --sft_only \
    --baseline_only \
    --run_tag epoch199 \

进度：
1.sum,qa效果好 有待测试：alpaca （可能是think链相似的原因）
2.将已有数据集翻译成小语种 有待测试：sum-威尔士语，阿姆哈拉语，蒙古语 目前发现翻译质量差，直接换现有数据集
3.寻找其他小众的可能有效领域：CNN/DailyMail XL-Sum GovReport BillSum MeQSum MLSUM ECtHR EURLEX LexGLUE （法律，文学）
4.写pipeline，正常推理->发现al下降->分割train set，选取若干条query微调训练
