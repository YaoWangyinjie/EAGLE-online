CUDA_VISIBLE_DEVICES=0 python gen_sft_data.py \
    --dataset humaneval \
    --num_samples_per_question 1 \
    --temperature 0.0 \
    --output_file data/humaneval_sft_v1.jsonl \
    --max_new_tokens 4096

注意最大tokens数会导致部分推理答案被截断，检查生成的jsonl文件

CUDA_VISIBLE_DEVICES=0 python sft_main.py \
    --dataset humaneval \
    --num_epochs 200 \
    --lr 1e-5 \
    --save_every 20 \
    --train_id 0 \
    --resume_from auto \
    --trainpath data/humaneval_sft_v1.jsonl \
    --max_len 4096 \

CUDA_VISIBLE_DEVICES=0 python eval_sft.py \
    --dataset humaneval \
    --sft_eagle checkpoints/humaneval/0_1e-5_epoch_199 \
    --sft_only \
    --baseline_only \
    --run_tag epoch199 \

进度：
1.mt_bench先前训练使用的生成答案最大tokens数1024太短，采用16384重新生成仍有12条截断
2.humaneval过滤出的没有截断的有63条，实际训练的57条有点少
3.EAGLE_online部分检查<think>的问题
4.怀疑是think链相似等原因，sum等数据集效果明显更好，尝试qa/alpaca
5.做小语种数据集，找法律/类似的文学数据集
6.写pipeline，正常推理->发现al下降->分割train set，选取若干条query微调训练
